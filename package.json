{
  "name": "auth-system",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "set NODE_ENV=test && set DATABASE_URL=postgresql://postgres:werio456@localhost:5432/auth_db_test && set JWT_SECRET=secret_untuk_testing && jest --runInBand",
    "test:watch": "set DATABASE_URL=postgresql://postgres:werio456@localhost:5432/auth_db_test && set JWT_SECRET=secret_untuk_testing && jest --watch --runInBand"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/client": "^6.19.2",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.3.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "prisma": "^6.19.2",
    "winston": "^3.19.0"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  }
}
